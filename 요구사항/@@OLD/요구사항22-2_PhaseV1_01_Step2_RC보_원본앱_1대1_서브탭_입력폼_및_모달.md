# 요구사항 22-2: Phase V1-1 Step 2 RC 보 원본앱 1:1 서브탭 입력폼 & 모달 명세서

## 1. 개요 및 원본 리소스 매핑

본 문서는 **RC 보 (`rc_beam`)**의 **Step 2 (원본앱 1:1 서브탭 입력폼 & 대화창 모달)** 구현을 위한 상세 요구사항 명세서입니다.
원본앱의 `IDD_RCS_BEAM_PMODE_DLG`의 대화상자 인터페이스를 현대적인 웹 컴포넌트로 1:1 완벽 이식합니다.

* **부재 및 모듈 식별자**: `rc_beam` (카탈로그 No. 1, Tier 1 플래그십)
* **담당 소스 파일**:
  - `src/web/static/js/components/form_rc_beam.js` (신규 1:1 서브탭 폼 컴포넌트)
  - `src/web/static/js/dialogs.js` (보 전용 배근 상세 모달 및 T형 플랜지 보조 모달 등록)
  - `src/web/static/js/form_dispatcher.js` (WIP 분기 해제 및 보 폼 마운트)
* **권장 AI 모델**: ⚙️ **Medium** - DOM 조작, 탭/테이블 UI, 이벤트 바인딩, 입력 유효성 검사 최적화
* **2순위 원본 리소스 SSOT**:
  - `original_src/Midas Design+/Language/Korean/DLG_DPLUS_RCS.ini`
  - 대화상자 리소스: `IDD_RCS_BEAM_PMODE_DLG`, `IDD_RCS_DEFL_DLG`
  - 배근 상세 대화상자: `IDD_RCS_BEAM_REBAR_DLG`

---

## 2. 원본앱 1:1 4대 서브탭 UI 구조

`form_rc_beam.js`는 원본 원본앱의 다이얼로그 계층을 1:1로 웹 서브탭 인터페이스로 완벽 재현합니다:

```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│ [RC 보 설계/검토 입력창: IDD_RCS_BEAM_PMODE_DLG]                                       │
├───────────────┬───────────────┬───────────────────────┬────────────────────────────────┤
│ Tab 1: 단면/재료 │ Tab 2: 철근배근 │ Tab 3: 부재력/하중조합 │ Tab 4: 사용성/처짐설정           │
└───────────────┴───────────────┴───────────────────────┴────────────────────────────────┘
```

### 2.1. Tab 1: 단면 및 재료 (Section & Material)
* **단면 형상 라디오 버튼**:
  - `IDC_RADIO_SHAPE_RECT`: 직사각형 보 ($b \times h$)
  - `IDC_RADIO_SHAPE_TEE`: T형 보 ($b_w, h, b_e, h_f$) - 선택 시 플랜지 입력란 동적 활성화
* **기하 치수 입력 필드**:
  - `width` ($b_w$): 복부 폭 (기본값: 400 mm)
  - `height` ($h$): 보 전체 춤 (기본값: 600 mm)
  - `length` ($L$): 유효 경간 (기본값: 6,000 mm)
  - `cover`: 인장측 순피복 두께 (기본값: 40 mm)
  - `cover_top`: 압축측 순피복 두께 (기본값: 40 mm)
  - `flange_width` ($b_e$): 플랜지 유효폭 (T형 선택 시 활성화, 기본값: 1,200 mm)
  - `flange_thickness` ($h_f$): 슬래브 두께 (T형 선택 시 활성화, 기본값: 150 mm)
* **재료 물성치 드롭다운 (KDS 표준 규격)**:
  - 콘크리트 강도 ($f_{ck}$): C21, C24, C27, C30, C35, C40, C50, C60 (기본: C27)
  - 주철근 강도 ($f_y$): SD400, SD500, SD600 (기본: SD400)
  - 전단철근 강도 ($f_{yt}$): SD400, SD500 (기본: SD400)
* **지지 조건 드롭다운**:
  - 단순지지 (Simple), 일단연속 (One-end Continuous), 양단연속 (Both Continuous), 캔틸레버 (Cantilever)

### 2.2. Tab 2: 철근 배근 (Reinforcement)
단부(End-I), 중앙부(Center-M), 단부(End-J)의 3구역 독립 배근 테이블을 제공합니다:

```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│ [위치별 주철근 및 스터럽 배근 테이블]                                                   │
├─────────┬──────────────────────────┬──────────────────────────┬────────────────────────┤
│ 위치     │ 상부 주철근 (Top Bars)   │ 하부 주철근 (Bot Bars)   │ 전단 스터럽 (Stirrup)  │
├─────────┼──────────────────────────┼──────────────────────────┼────────────────────────┤
│ End-I   │ 1단: 4-D25, 2단: 2-D25   │ 1단: 3-D22               │ D10 @150 (2 Legs)      │
│ Center-M│ 1단: 2-D22               │ 1단: 4-D25, 2단: 2-D25   │ D10 @250 (2 Legs)      │
│ End-J   │ 1단: 4-D25, 2단: 2-D25   │ 1단: 3-D22               │ D10 @150 (2 Legs)      │
└─────────┴──────────────────────────┴──────────────────────────┴────────────────────────┘
```
* **철근 호칭경 콤보박스**: D10, D13, D16, D19, D22, D25, D29, D32
* **다단 배근 지원**: 1단 및 2단 분리 입력 지원, 단간 순간격 25mm 자동 반영
* **비틀림 보강근 입력**:
  - 측면 종방향 비틀림 철근 호칭경 및 단면당 총 개수 (예: 4-D13)
* **배근 상세 설정 모달 버튼 (`...`)**: 클릭 시 `IDD_RCS_BEAM_REBAR_DLG` 모달 호출

### 2.3. Tab 3: 설계 부재력 및 하중조합 (Design Factored Loads)
* **단부 및 중앙부 계수하중 입력 테이블**:
  - $M_u^+$ (정모멘트, kN·m)
  - $M_u^-$ (부모멘트, kN·m)
  - $V_u$ (계수전단력, kN)
  - $T_u$ (계수비틀림모멘트, kN·m)
* **하중조합 일괄 입력 및 최악 LCB 선택 기능 지원**

### 2.4. Tab 4: 사용성 및 처짐 설정 (Serviceability & Deflection)
* **처짐 산정용 사용하중 입력**:
  - 사용 모멘트 ($M_{a,pos}, M_{a,neg}$): kN·m
  - 지속하중에 의한 모멘트 ($M_{sus}$): kN·m
* **처짐 한계비 선택**: $L/240$, $L/360$, $L/480$ (기본값: $L/240$)
* **노출 환경 등급 선택 (균열폭 허용치 $w_{lim}$ 결정)**:
  - 건조 환경 ($w_{lim} = 0.4\text{ mm}$)
  - 습윤 환경 ($w_{lim} = 0.3\text{ mm}$, 기본값)
  - 부식성 환경 ($w_{lim} = 0.2\text{ mm}$)

---

## 3. 상세 대화창(`...`) 서브 모달 명세

### 3.1. 배근 상세 설정 모달 (`IDD_RCS_BEAM_REBAR_DLG`)
* **갈고리 및 정착 형식**:
  - 주철근 단부 갈고리 (90° 표준갈고리 / 180° 반원형 갈고리)
  - 스터럽 갈고리 (135° 내진 갈고리 / 90° 표준 갈고리)
* **스터럽 다리수 상세 구성**:
  - 2각 (외곽 폐합 1개)
  - 4각 (외곽 폐합 1개 + 내부 U형 묶음 또는 크로스타이)
* **순간격 검토 옵션**: 철근 순간격 $s_{clear} \ge \max(25\text{ mm}, d_b, 1.33 \times d_{agg})$ 자동 경고

### 3.2. T형 플랜지 유효폭 자동 보조 모달
* **KDS 14 20 20 기준 유효플랜지폭 $b_e$ 자동 계산기**:
  - 경간 조건: $L / 4$
  - 인접 보 거리: $b_w + s_1/2 + s_2/2$
  - 슬래브 두께: $b_w + 16 h_f$
  - 위 3가지 조건 중 최솟값 자동 산출 및 폼 주입 버튼 제공

---

## 4. 프론트엔드 컴포넌트 아키텍처 및 이벤트 연동

1. **원본앱 3대 액션 버튼 파이프라인 (`input-action-bar`)**:
   - **[💾 적용 (Apply)] 버튼**: 현재 폼의 모든 입력 데이터를 `ProjectStore` 메모리에 기록하고 캔버스 그래픽 동기화만 수행 (**계산서는 실시간 갱신하지 않음**).
   - **[⚡ 검토 (Check)] 버튼**: 선행 적용 후 KDS 백엔드 검토 API(`/api/rc/beam/check`)를 호출하여 **오직 검토 버튼 클릭 시에만** Pane 4 계산서 및 DCR 안전율 갱신.
   - **[✨ 자동설계 (Auto Design)] 버튼**: 현재 단면($b, h$), 재료 강도($f_{ck}, f_y, f_{yt}$), 계수하중($M_u, V_u$)에 대해 KDS 최적 철근 자동설계 알고리즘(`/api/rc/beam/auto-design`)을 구동하여 최적 배근 산출 후 폼 필드 자동 주입 및 메모리 동기화.
2. **상태 관리 연동 (`ProjectStore`)**:
   - 디바운스(Debounce 50ms)를 적용하여 폼 입력 시 메모리 및 캔버스 뷰만 가볍게 동기화하고 과도한 계산서 재연산 방지.
3. **UI/UX 디자인 시스템 및 반응형 방어 규약**:
   - **패널 축소 시 폼 찌그러짐 방지**: 좌측 서브패널 최소너비 `min-width: 320px` 보장, 인풋 컨트롤 최소너비 `110px`, 2열/3열 그리드는 `repeat(auto-fit, minmax(220px, 1fr))`로 축소 시 자연스러운 1열 래핑 보장, 테이블 영역 `overflow-x: auto` 가로 스크롤로 컨트롤 손상 원천 방어.
   - **상부 서브탭 반응형 래핑**: 패널 너비 변경 시 서브탭 버튼이 잘리지 않도록 `flex-wrap: wrap;` 및 반응형 패딩 적용. 모듈 주도형 서브탭 구성 준수.
   - **테마 무결성 (Dark/Light)**: 폼 및 테이블의 모든 색상/배경을 CSS 테마 변수(`var(--bg-...)`, `var(--text-...)`, `var(--border)`)로 일원화하여 🌙/☀️ 테마 전환 시 하드코딩 없는 선명한 가독성 유지.
   - **엔지니어링 폼 폰트 & 스타일 통일**: 본문 `'Inter'`, 헤더 `'Outfit'`, 모노스페이스 `'Fira Code'`, 4~6px 라운드 및 일관된 콤보박스 디자인 유지.

---

## 5. 검증 및 수용 기준 (DoD)

- [x] `src/web/static/js/components/form_rc_beam.js` 컴포넌트 구현 완료.
- [x] 4대 서브탭(단면/재료, 철근배근, 부재력, 사용성) 100% 렌더링 및 너비 축소 시 래핑 검증.
- [x] 원본앱 3버튼 툴바(`[💾적용] [⚡검토] [✨자동설계]`) 연동 및 검토 시에만 계산서 갱신 검증.
- [x] 자동설계 버튼 클릭 시 현재 단면/하중에 적합한 최적 철근 배근 자동 산출 및 폼 주입 검증.
- [x] 단부(I/J) 및 중앙부(M) 배근 테이블 및 T형 단면 전환 기능 정상 작동.
- [x] 상세 배근 모달(`...`) 팝업 및 데이터 양방향 바인딩 정상 작동.
- [x] 다크/라이트 테마 전환 시 폼/테이블/입력창 색상 반전 무결성 확보.
- [x] 브라우저 콘솔 에러 0건 및 렌더링 지연 없음.
