# 요구사항 22-5: Phase V1-1 Step 5 RC 보 4열 통합 E2E 검증 & 실사용 UI 온라인 전환 명세서

## 1. 개요 및 최종 통합 목표

본 문서는 **RC 보 (`rc_beam`)**의 **Step 5 (4열 통합 E2E 검증 & 실사용 UI 온라인 전환)** 구현을 위한 상세 요구사항 명세서입니다.

* **부재 및 모듈 식별자**: `rc_beam` (카탈로그 No. 1, Tier 1 플래그십)
* **담당 소스 파일**:
  - `src/web/static/js/catalog.js` (`rc_beam`의 `is_wip: false` 전환 및 온라인 등록)
  - `src/web/static/js/app.js` (전체 4열 오케스트레이션 및 이벤트 버스)
  - `tests/e2e/test_rc_beam_e2e.py` (브라우저 자동화 E2E 통합 테스트)
* **권장 AI 모델**: ⚙️ **Medium** - 통합 이벤트 버스 점검, E2E 시나리오 테스트, WIP 플래그 관리
* **통합 목표**:
  - **4-Pane 워크스페이스(사이드바 ── Left-Sub(부재/폼) ── 중앙 그래픽 ── 우측 순백색 A4 계산서)**의 **100ms 이내 실시간 3-View 동시 동기화**.
  - 기존의 WIP 임시 안내 카드를 완전히 걷어내고 실무 구조 엔지니어가 즉시 실무에 투입할 수 있는 완성형 웹 부재설계 소프트웨어로 정식 릴리즈.

---

## 2. 4-Pane 워크스페이스 실시간 3-View 동기화 파이프라인

```
┌─────────────────────────────────────────────────────────────────────────────────────────────────┐
│ AltDP_3rd RC 보 4-Pane 워크스페이스 실시간 동기화 아키텍처 (ProjectStore 기반)                  │
├─────────────────┬───────────────────────────────┬───────────────────────┬───────────────────────┤
│ [1열: 사이드바] │ [2열: Left-Sub (부재/폼)]     │ [3열: Center 그래픽]  │ [4열: Right 계산서]   │
│  RC > 보 선택   │  • Pane 1: 다중 부재 매니저   │  • 상단: 종단면/부재력│  - 5대 장구분 계산서  │
│  (Online 활성)  │  • Pane 2: 4대 서브탭 입력폼  │  • 하단: 3대 횡단면도 │  - 8단계 KaTeX 전개   │
│                 │    - 단면/재료 (KS 콤보박스)  │  - 135° 갈고리 스터럽 │  - 순백색 A4 프리뷰   │
│                 │    - 철근배근 (모달 연동)     │  - 철근태그/치수선    │  - O.K / N.G 상태     │
│                 │    - 부재력 / 사용성 처짐균열 │  - 휠 줌/팬/Fit/호버  │  - 인쇄/PDF/Excel     │
└─────────────────┴───────────────────────────────┴───────────────────────┴───────────────────────┘
                                ▲                         ▲                        ▲
                                │ (Input Event)           │                        │
                                └────── [ProjectStore] ───┴────────────────────────┘
                                      (State Update <= 50ms)
```

### 2.1. 실시간 반응 속도 기준
- 2열 Left-Sub 입력폼에서 보 단면 치수($b_w, h$), 주철근 호칭경 및 개수, 스터럽 간격, 부재력 등을 수정하거나 조작할 때:
  1. `ProjectStore` 상태 갱신: $\le 10\text{ ms}$
  2. 파이썬 백엔드 또는 웹 워커 KDS 보 해석 재계산: $\le 40\text{ ms}$
  3. 3열 캔버스 종단면 및 3대 횡단면 리드로우: $\le 30\text{ ms}$
  4. 4열 A4 계산서 KaTeX 수식 재렌더링: $\le 50\text{ ms}$
  - **총 동기화 소요 시간 $\le 100\text{ ms}$**로 끊김 없는 인터랙티브 설계 환경 확보.

---

## 3. WIP 해제 및 온라인 카탈로그 전환 규약

1. **`src/web/static/js/catalog.js` 메타데이터 업데이트**:
   ```javascript
   {
       id: "rc_beam",
       name: "RC 보 (Beam)",
       category: "rc",
       tier: "Tier 1",
       is_wip: false,               // false로 전환하여 정식 온라인 오픈
       status: "Online",            // "WIP" -> "Online"
       icon: "icon-beam",
       component: "form_rc_beam",
       renderer: "vector_rc_beam",
       report: "redcr_rc_beam"
   }
   ```
2. **UI 배너 및 안내 카드 청산**:
   - `rc_beam` 선택 시 더 이상 `wip_placeholder`나 경고 배너가 노출되지 않고, 온전한 원본앱 1:1 서브탭 폼과 캔버스 및 계산서가 즉각 로드됨.

---

## 4. E2E 통합 테스트 시나리오 및 검증 (`tests/e2e/test_rc_beam_e2e.py`)

1. **시나리오 1: 사이드바 네비게이션 및 모듈 마운트**:
   - 1열 사이드바에서 `RC 보` 클릭 시 2열 Left-Sub에 4대 서브탭 폼이 마운트되고, 3열에 세로 적층 캔버스 뷰포트가 생성되며, 4열에 계산서가 로드되는지 검증.
2. **시나리오 2: 파라메트릭 입력 및 3-View 실시간 동기화**:
   - 폼에서 폭 $b_w$를 400에서 500으로 변경 시 3열 캔버스 치수선이 500으로 갱신되고 4열 계산서 제1장 및 휨모멘트 강도가 실시간 재계산되는지 확인.
3. **시나리오 3: 2D 캔버스 인터랙션**:
   - 캔버스 휠 줌, 드래그 팬 시 에러 없이 변환 행렬이 적용되는지 검증.
4. **시나리오 4: A4 계산서 인쇄 프리뷰**:
   - 인쇄 버튼 클릭 시 미디어 쿼리(`@media print`)가 적용된 순백색(`#ffffff`) A4 프리뷰 윈도우가 정상 호출되는지 검증.

---

## 5. 검증 및 수용 기준 (DoD)

- [ ] `src/web/static/js/catalog.js`에서 `rc_beam`의 `is_wip: false` 전환 완료.
- [ ] 4-Pane 워크스페이스 실시간 100ms 동기화 확인.
- [ ] E2E 통합 테스트 전 시나리오 통과.
- [ ] 브라우저 개발자 도구 콘솔 에러 0건.
- [ ] `docs/16`에 따른 4대 물리적 증거 첨부 및 최종 Git 커밋/푸시 완료.
