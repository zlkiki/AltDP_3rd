# 12. Web Application UI/UX 종합 명세서 (12_web_application_ui_ux_specification.md)

본 문서는 **AltDP_3rd 모던 웹 부재설계 플랫폼**의 프론트엔드 아키텍처, 4-Pane 반응형 레이아웃, 독립 드래그 리사이저 엔진, 중앙 데이터 스토어(SSOT), 다중 단위계 관리, 파라메트릭 인풋 폼, 2D/3D 벡터 그래픽 엔진, 순백색 A4 KDS 구조계산서 렌더러와 **전 부재 공통 Web UI/UX 표준 단일 진실 공급원(SSOT) 종합 명세서**입니다.

> [!NOTE]
> **🏛️ 원본 데스크톱앱 역공학 기술 명세서 분리 안내**:  
> 원본 데스크톱앱(`Design+.exe`)의 MFC/BCGControlBar 윈도우 프레임워크, `Menu.ini` 11개 탭 리본 메뉴, `DLG_*.ini` 다이얼로그 리소스 및 C++ 심볼 역공학 분석은 전용 기술 문서인 **[`docs/54_original_app_ui_specification.md`](54_original_app_ui_specification.md)**에서 단독 관리합니다.

```
┌──────────────────────────────────────────────────────────────────────────────────────────────────┐
│ 12_web_application_ui_ux_specification.md 순수 Web UI/UX 핵심 구조도                            │
├──────────────────────────────────────────────────────────────────────────────────────────────────┤
│ [PART 1] AltDP_3rd 모던 웹 UI/UX 아키텍처 및 디자인 시스템                                      │
│   • 4대 핵심 UX 설계 철학 | 상단 마스터 툴바 | 스마트 사이드바 & 8대 카테고리/즐겨찾기           │
│   • 4-Pane 워크스페이스 (부재목록, 4대서브탭 폼, 세로적층 다중뷰포트, 순백색 A4 계산서)          │
│   • 요구사항 22 UI 규격 (배근유형 라디오, 주근 분리입력, 12포인트 순간격 뱃지, 부재력 disabled)   │
│   • 4대 독립 리사이저 | 디자인 토큰 | 키보드 단축키 | 다형적 모듈팩 프론트엔드 아키텍처          │
├──────────────────────────────────────────────────────────────────────────────────────────────────┤
│ [PART 2] 원본 ↔ 모던 웹 1:1 매핑 핵심 원칙 및 브릿지                                             │
│   • 원본 4대 폼뷰 & 3대 인터랙션 모드 1:1 웹 구현 매핑 (상세 기술 명세: docs/13 참조)           │
├──────────────────────────────────────────────────────────────────────────────────────────────────┤
│ [PART 3] 부재별 수직 관통(Vertical Slice) 5대 공정과 UI/UX 연동 표준 (요구사항 21/22 기반)     │
│   • 2단계 5정밀 마이크로 공정(Step 1~5)과 UI/UX 레이어 1:1 바인딩                                │
│   • 부재별 수직 구현계획(요구사항 문서) 수립 시 UI/UX 필수 검토 항목 (Checklist)                 │
└──────────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

# [PART 1] AltDP_3rd 모던 웹 UI/UX 아키텍처 (우리 앱 우선)

## 1. 개요 및 4대 핵심 UX 설계 철학 (Design Philosophy)

AltDP_3rd는 건축 구조 엔지니어가 4대 카테고리(RC, Steel, SRC/PC, Misc/RFM) 54개 단위부재의 단면 내력 검토 및 최적 배근 설계를 웹 브라우저에서 쾌속으로 수행할 수 있도록 설계된 **Zero-Build 4분할(4-Pane) 반응형 올인원 웹 애플리케이션**입니다.

### 🌟 4대 핵심 UX 원칙
1. **무결한 시인성 (Flawless Legibility & Fixed White Sheet)**:
   - 작업 인터페이스는 **다크 테마(Dark Mode)**와 **라이트 테마(Light Mode)**를 자유롭게 전환.
   - 단, **KDS 구조계산서 영역은 실제 A4 인쇄물과의 1:1 완벽 호환을 위해 테마와 무관하게 언제나 순백색(`#ffffff`) 배경과 고대비 텍스트(`#111827`)**를 고정 유지.
2. **독립 4분할 워크스페이스 (Isolated 4-Split Layout)**:
   - 탐색기(사이드바), 부재 매니저(Pane 1), 파라메트릭 폼(Pane 2), 세로 적층형 그래픽 뷰포트(Pane 3), KDS 계산서(Pane 4)가 드래그 시 서로 연쇄 간섭 없이 독립적으로 크기 조절.
3. **스마트 자동 숨김 & 즐겨찾기 고정 인터랙션 (Smart Collapsible Sidebar & Favorites)**:
   - 8대 카테고리 Pills 탭 필터와 상시 고정 **[⭐ 즐겨찾기]** 시스템을 제공하며, 집중 모드 시 3초 무조작 및 마우스 이탈 시 사이드바가 자동으로 접히고, 고정핀(📌)을 꽂으면 상시 고정 유지 (`Ctrl + B` 토글 지원).
4. **3버튼 통합 액션 파이프라인 (Optimized Action Pipeline)**:
   - **[💾 적용 (Apply)]**, **[⚡ 검토 (Check)]**, **[✨ 설계 (Design)]** 단일화 파이프라인 제공 (검토/설계 시 항상 적용 선행 실행).

```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ Top Master Toolbar : 브랜드/KDS 기준 태그 | 🌐 단위계(SI/MKS/US) | 테마(🌙/☀️) | 💾 프로젝트 저장/열기 | 🔄 레이아웃 리셋  │
├────────────────────────┬───────────────────────────────┬───────────────────────────────┬───────────────────────────────┤
│ [Left Sidebar]         │ [Left-Sub Pane]               │ [Center Pane]                 │ [Right Pane]                  │
│ 📂 스마트 모듈 탐색기 │ ┌───────────────────────────┐ │ ┌───────────────────────────┐ │ ┌───────────────────────────┐ │
│  • 8대 카테고리 Pills  │ │ Pane 1: 다중 부재 매니저  │ │ Pane 3: 그래픽 정보부       │ │ Pane 4: KDS 구조계산서      │ │
│  • ⭐ 즐겨찾기 상시 고정│ │  • 부재 리스트 (+추가/복제) │ │ (세로 적층형 다중 뷰포트)   │ │  • 상시 순백색(#ffffff) A4  │ │
│  • 3단계 계층 WorkTree  │ │  • 단면/배근/DCR 요약 그리드│ │  [상단 뷰포트: 단면/형상]   │ │  • [상세(•)][요약( )] 라디오│ │
│  • DCR 상태 글자 (OK/NG)│ ├───────────────────────────┤ │  [하단 뷰포트: 역학/P-M/거동│ │  • [☑ 입력 데이터 상세 포함] │ │
│  • 📌 고정핀 / 자동숨김 │ │ Pane 2: 파라메트릭 입력폼  │ │  • 전 부재군(RC/Steel/PC/..)│ │  • [머릿말/회사명 설정 ⚙️]  │ │
│                        │ │  • 부재 고유 4대 서브탭   │ │  • 줌/팬/Fit/호버 툴팁      │ │  • KaTeX 8단계 수식 유도    │ │
│                        │ │  • KS 콤보박스 & 서브모달 │ │                             │ │  • 원본  →  O.K 판정 표기   │ │
│                        │ │  • [💾적용] [⚡검토] [✨설계]│ │                             │ │  • [인쇄] [PDF] [Excel]     │ │
│                        │ └───────────────────────────┘ │ └───────────────────────────┘ │ └───────────────────────────┘ │
├────────────────────────┴───────────────────────────────┴───────────────────────────────┴───────────────────────────────┤
│ 4대 독립 리사이저 (`layout_resizer.js`): resizer-sidebar-h ── resizer-left-h ── resizer-left-v ── resizer-main-h      │
└────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 상단 마스터 툴바 (Top Master Toolbar)

상단 툴바는 전역 설정 및 빠른 도구 기능을 제공합니다:

1. **브랜드 & 기준 배지**:
   - `AltDP Member Designer` 로고 + `KDS 2022 / KDS 14 20 00 / 14 31 00` 기준 태그.
2. **사이드바 토글 버튼 (Ctrl+B)**:
   - 좌측 네비게이터를 즉시 열고 닫는 단축키 및 토글 버튼.
3. **초고속 모듈 검색바 (Ctrl+K, `#quick-search`)**:
   - 54종 단위부재(보, 기둥, 전단벽, 슬래브, 기초, H형강, 접합부 등) 퍼지 검색 및 즉시 활성화.
4. **4대 단위계 실시간 전환기 (`unit_manager.js`)**:
   - `SI`: $\text{kN, mm, MPa, kN}\cdot\text{m}$ (기본값)
   - `SI_M`: $\text{kN, m, kPa, kN}\cdot\text{m}$
   - `MKS`: $\text{tonf, m, kgf/cm}^2\text{, tonf}\cdot\text{m}$
   - `US`: $\text{kip, in, ksi, ft}\cdot\text{kip}$
   - *단위계 변경 시 입력폼 값, 캔버스 치수, 계산서 수식이 0.01초 내 자동 변환 및 유효숫자 포맷팅.*
5. **테마 & 레이아웃 관리**:
   - **다크/라이트 테마 (🌙/☀️)**: 고대비 엔지니어링 테마 토글 (`theme_manager.js`).
   - **레이아웃 기본값 저장 (`📌 Save Layout`)**: 분할 비율 및 사이드바 상태를 `localStorage`에 영속화.
   - **레이아웃 초기화 (`🔄 Reset Layout`)**: 기본 황금비 분할로 원클릭 복원.

---

## 3. 계층형 모듈 탐색기 및 스마트 사이드바 (Left Sidebar: `#sidebar-nav`)

스마트 사이드바는 번잡한 상단 검색창/칩을 덜어내고 사이드바 내에 **초고접근성 3중 필터 네비게이션**과 즐겨찾기 시스템을 제공합니다:

### 3.1. 8대 카테고리 Pills 탭 필터 (Category Pills)
* **[전체보기 (ALL)]**: 61종 전체 모듈 전개.
* **[즐겨찾기 (⭐ Favorites)]**: 사용자가 즐겨찾기 등록한 모듈 및 활성 부재만 최상단 집중 표시.
* **[RC 콘크리트]**: 보, 기둥, 전단벽, 슬래브, 기초, 옹벽, 지하외벽, 앵커볼트, 코벨/브라켓.
* **[STEEL 강구조]**: 철골보, 철골기둥, 가새, 트러스, 크레인거더, 접합부 5종, 베이스플레이트.
* **[SRC / PC]**: SRC 기둥/보, CFT 기둥, 프리캐스트 보/슬래브, 더블티, 할로우코어.
* **[ALU 알루미늄]**: 알루미늄 보/기둥, 커튼월 멀리온, 루버 프레임.
* **[RFM 보강]**: CFRP 탄소섬유판/시트 보강, 강판 접착 보강, 콘크리트 단면증설.
* **[기초 / 특수]**: 독립/복합/줄기초, 말뚝기초, 평판 FEM 매트기초.

### 3.2. 즐겨찾기(Favorites ⭐) 상시 고정 시스템
* 각 모듈 및 부재명 우측에 별표 아이콘(`☆`/`★`) 제공.
* 클릭 시 `localStorage`에 영속화되며 사이드바 최상단 `[⭐ 즐겨찾기]` 그룹에 상시 고정되어 실무 빈출 부재(RC보, 기둥, H형강보 등)로 1클릭 즉시 전환.

### 3.3. 3단계 계층 WorkTree 및 DCR 2단계(OK/NG) 글자 표기
* **Level 1 (대분류)** $\rightarrow$ **Level 2 (단위 모듈 & 등록 부재 수 `(N)`)** $\rightarrow$ **Level 3 (개별 등록 부재 `1F-B1` 등)**.
* **DCR 상태 2단계 텍스트 표기 (칩/배경색 제거, 폰트 컬러로만 표기)**:
  - **OK (`DCR ≤ 1.000`)**: **녹색 글자 (`OK`)**로만 깔끔하게 표기.
  - **NG (`DCR > 1.000`)**: **빨간색 글자 (`NG`)**로만 표기. 클릭 시 해당 부재로 즉시 전환되고 4열 계산서의 불합격 수식 위치로 자동 스크롤.
* **스마트 자동 숨김 및 고정핀 (📌)**:
  - 집중 모드 시 3초 무조작 및 마우스 이탈 시 슬라이드 수축 (`Ctrl + B` 단축키 토글 지원).
  - 고정핀(📌)을 클릭하면 워크스페이스 패널로 상시 고정.
* **프로젝트 파일 입출력 (Project I/O)**:
  - `💾 저장 (Export)`: 프로젝트 내 모든 부재 데이터를 단일 `.json` 파일로 다운로드 (`Ctrl + S`).
  - `📂 불러오기 (Import)`: 저장된 JSON 파일을 로드하여 다중 부재 상태 완벽 복원.

---

## 4. 4분할 작업 영역 상세 명세 (4-Pillars)

### 4.1. Pane 1: 다중 부재 매니저 (Top-Left: `#pane-member-list`)
* **컴포넌트**: `member_manager.js`
* **위치 및 크기**: Left-Sub 패널 상단 (높이: 80px ~ 400px 상하 수직 리사이저 `resizer-left-v`로 가변 조절).
* **주요 기능**:
  - **부재 인스턴스 CRUD**: `[+ 추가 (Add)]`, `[복제 (Duplicate)]`, `[삭제 (Delete)]` 툴바 액션.
  - **인라인 부재명 변경**: 더블클릭 또는 인라인 편집으로 `1F-B1`, `Roof-G2` 등 신속 변경.
  - **실시간 요약 스프레드시트 테이블**:
    | 부재명 | 단면 치수 ($b \times h$) | 주요 배근 / 형강 규격 | 소요력 ($M_u, V_u$) | DCR 상태 (글자 표기) |
    |:---:|:---:|:---:|:---:|:---:|
    | **1F-B1** | $400 \times 600$ | 상 4-D25, 하 4-D25 | $210 \text{ kN}\cdot\text{m}$ | **0.807 OK** (녹색 글자) |
    | **1F-B2** | $500 \times 700$ | 상 6-D25, 하 6-D25 | $450 \text{ kN}\cdot\text{m}$ | **1.052 NG** (빨간색 글자) |
  - 행 클릭 시 0.01초 내 해당 부재의 입력폼, 그래픽 뷰, 계산서가 즉시 동기화 전환.

### 4.2. Pane 2: 파라메트릭 사용자 입력부 (Bottom-Left: `#pane-input-form`)
* **컴포넌트**: `form_generator.js`, `form_combobox.js`, `modal_manager.js`
* **위치**: Left-Sub 패널 하단.
* **주요 구성**:
  - **2단 적층형 헤더 (Vertical Stack Header)**:
    - 1행: `⚙️ 사용자 입력부 (Input)` 타이틀 바 및 활성 부재 뱃지(`[1F-B1]`).
    - 2행: 사용자 입력부 가로 폭(100%)을 사용하는 전용 모듈 경로 배너 (`RC › 보 (Beam) › 직사각형 보`).
  - **모듈 주도형 가변 서브탭 표준 아키텍처**:
    - 사용자 입력부는 **통일된 다크/라이트 테마와 정밀 엔지니어링 UX를 공유**하되, **상부 서브탭의 개수(1~5개)와 명칭, 입력받는 데이터 제원 및 형태는 원본앱(`DLG_*.ini`)으로부터 도출하여 각 설계 모듈에서 직접 정의/주입**.
    - 공통 서브탭 구성 가이드라인:
      1. **[단면 / 재료 탭]**: 단면 기하 치수, 재료 강도 등급, 피복두께 등 기본 물리 제원.
      2. **[배근 / 상세 탭]**: 단면별 주철근 및 전단/횡보강근, 스티프너, 볼트/용접 등 구조 상세.
      3. **[부재력 / 하중 탭]**: 설계 계수 부재력(축력, 휨모멘트, 전단력, 비틀림 등) 및 하중조합.
      4. **[사용성 / 설계옵션 탭]**: 사용하중, 처짐/균열 한계치, 노출 환경, 내진 상세 범주.
      *(※ RC 보 전용 4대 서브탭 상세 및 3-Station 배근 UI는 [`docs/15_rc_beam_specification.md`](15_rc_beam_common_requirements_report.md) 참조)*
  - **다중 단면 배근 및 대칭 동기화 공통 인터랙션 표준**:
    - 선형 부재(보, 기둥, 거더 등)에서 다중 단면(단부 및 중앙부)을 다룰 때, 사용자의 반복 입력을 최소화하기 위한 **동기화 모드(전단면 일괄, 양단부 대칭 연동, 전단면 독립) 라디오 컨트롤**을 표준으로 지원합니다.
    - 대칭 연동 시 종속 단면의 셀은 시각적 안내 태그와 함께 `disabled` 처리되어 휴먼 에러를 방지합니다.
  - **복합 분리입력 컨트롤 (Composite Input Control) UI 표준**:
    - 단일 텍스트박스 타이핑 오류를 방지하기 위해 드롭다운 콤보와 개수 입력이 결합된 **`[개수 입력] + [구분자] + [규격/호칭경 콤보]`** 3분할 인라인 복합 컨트롤을 표준 입력 패턴으로 채택합니다.
  - **실시간 기하 적합성 검토 뱃지 및 상세 팝오버 표준**:
    - 철근 순간격(Clear Spacing), 볼트 연단거리/피치, 판폭두께비 등 시공성/규준 한계치를 폼 조작 즉시 실시간 연산하여, **대표 거버닝 뱃지(O.K / N.G)** 및 클릭 시 전수 검토 그리드를 제공하는 팝오버를 공통 지원합니다. *(RC 보 12포인트 순간격 검토 상세는 [`docs/15`](15_rc_beam_common_requirements_report.md) 수록)*
  - **드래그 리사이징 반응형 레이아웃 방어 규약**:
    - 패널 좌우 리사이즈 드래그 시 텍스트박스나 콤보박스가 찌그러져 사용 불가능해지는 현상을 방지하기 위해:
      * **패널 최소 너비 강제 제약**: `min-width: 320px` 보장 (`layout_resizer.js` 및 CSS).
      * **컨트롤 최소 폭 및 반응형 래핑**: `min-width: 110px`, 그리드 레이아웃은 `grid-template-columns: repeat(auto-fit, minmax(130px, 1fr));` 및 유연한 래핑 적용.
      * 내부 가로 스크롤 `overflow-x: auto;`로 데이터 잘림 및 UI 붕괴 원천 차단.
  - **KS 표준 콤보박스 (`form_combobox.js`)**:
    - 단일 클릭 드롭다운 팝업 리스트 + 키보드 직접 타이핑 하이브리드 지원.
    - KS D 3504 이형철근(`D10`~`D57`), 콘크리트 강도(`fck`), 철근 강종(`SD400` 등), 강재 강종(`SM355` 등), 고력볼트(`F10T`), KS 형강 규격 DB 연동.
  - **원본 1:1 서브 대화창(`...`) 모달 시스템 (`ModalManager`)**:
    - `IDD_RCS_BEAM_BEFF_DLG`: T형/L형 플랜지 유효폭($b_{eff}$) 자동 계산 모달.
    - `IDD_RCS_COLUMN_SWAY_DLG`: 장주 모멘트확대계수($\delta_{ns}, \delta_s$) 산정 모달.
    - `IDD_STEEL_SECTION_DB_DLG`: KS H형강, 각형강관 단면성능치 브라우저 모달.
    - `IDD_LOAD_COMBINATION_DLG`: KDS 41 10 15 하중조합 자동 생성 모달.
  - **3버튼 통합 액션 툴바 & 단일화 파이프라인 (`IDC_DPLUS_INP_BTN_*` 원본 계승)**:
    - **[💾 적용 (Apply)] 버튼**: 현재 폼의 입력 데이터를 `ProjectStore` 메모리에 기록하고 캔버스 그래픽 동기화만 수행. **우측 기준검토부 계산서는 실시간 갱신하지 않음**.
    - **[⚡ 검토 (Check)] 버튼**: 항상 **[적용]**을 1순위로 선행 실행한 뒤, 백엔드 KDS 엔진을 호출하여 **오직 검토 버튼이 눌러질 때만** 우측 Pane 4 구조계산서 및 DCR 안전율을 계산 및 갱신. (입력 시 실시간 계산서 폭주 차단)
    - **[✨ 자동설계 (Auto Design)] 버튼**: 항상 **[적용]**을 1순위로 선행 실행한 뒤, 원본 소스 및 KDS 기준 엔진의 자동설계 알고리즘(`/api/rc/beam/auto-design`)을 구동하여 $DCR \le 1.0$ 만족 최적 철근 배근(단부/중앙부 상하부 주철근 및 스터럽 간격)을 자동 산출하여 폼에 즉시 주입.
  - **전 모듈 공통 상단 Sticky 플로팅 바 및 32px 컴팩트 버튼 표준 (Global Floating Bar & Compact Action Bar Standard)**:
    - **상단 Sticky 플로팅 고정**:
      - 긴 입력 폼 스크롤 시에도 3대 액션 버튼(`[💾 적용] [⚡ 검토] [✨ 자동설계]`)과 상부 서브탭 바가 화면 상단에 항시 고정 (`position: sticky; top: 0; z-index: 25; backdrop-filter: blur(8px); background: rgba(var(--bg-card-rgb), 0.95);`).
      - 54개 전 모듈의 입력 폼 최하단에서 작업 중이라도 상단으로 재스크롤할 필요 없이 언제든 1클릭으로 적용/검토/설계 파이프라인 트리거 보장.
    - **32px 컴팩트 엔지니어링 버튼 표준**:
      - 버튼 높이: `32px` (기존 42px에서 슬림화하여 상단 유효 작업 영역 극대화).
      - 패딩: `5px 12px` / 폰트: `12px` / `font-weight: 600`.
      - 모던하고 절제된 고품격 마이크로 호버 및 액티브 인터랙션 적용.
  - **입력 폼 UI/UX 컨셉 및 반응형 규약**:
    - **상부 서브탭 반응형 래핑**: 좌측 패널 폭 리사이즈 시 서브탭이 가려지거나 잘리지 않도록 `flex-wrap: wrap;` 및 반응형 너비(`min-width: 65px;`) 지원.
    - **테마 무결성 (Dark/Light)**: 폼 섹션, 입력창, 테이블, 콤보박스 모두 하드코딩 색상을 배제하고 전역 CSS 테마 변수(`var(--bg-...)`, `var(--text-...)`, `var(--border)`)를 100% 바인딩하여 🌙/☀️ 테마 전환 시 무결한 시인성 보장.
    - **엔지니어링 폼 스타일 일치**: `'Inter'` 본문 폰트, `'Outfit'` 섹션 헤더, `'Fira Code'` 숫자/코드 모노스페이스, 모던 라운드(`4px~6px`) 및 포커스 링 통일.

### 4.3. Pane 3: 동적 가변 다중 뷰포트 그래픽 정보부 (Center: `#pane-graphic-view`)
* **컴포넌트**: `graphic_viewport.js`, `renderer2d.js`, `pm_chart.js`, `canvas.css`
* **아키텍처 구조**:
  - 세로로 긴 종방향 뷰포트 특성을 극대화하여, 부재의 특성에 따라 **1단 ~ 3단 동적 가변 다중 뷰포트 (Vertical Multi-Card Stack Layout)** 구축.
  - **각 단의 명칭(Title Badge)과 툴바 액션은 원본앱(`original_src/Language/Korean/*.ini`)의 공식 용어를 참조하여 각 설계 모듈에서 동적으로 정의/주입**.
  - 원본앱이 가로로 긴 형태를 취하고 있더라도, 우리 웹의 세로 긴 그래픽 정보부에 최적화하여 **세로로 분할/병렬 배치**.
  - 카드별 독립 줌(Zoom)·팬(Pan), 뷰포트 맞춤(`[Fit]`) 제어기 탑재.

```
┌──────────────────────────────────────────────────────────────────────────────────────────────────┐
│ Center Graphic Viewport (동적 가변 1~3단 세로 적층형 다중 뷰포트)                                 │
├──────────────────────────────────────────────────────────────────────────────────────────────────┤
│ ┌──────────────────────────────────────────────────────────────────────────────────────────────┐ │
│ │ [1단 뷰포트: 원본앱 참조 모듈 정의 타이틀 (예: 단부( i ) 단면)]        [Fit] [Zoom] [Dim 켜기]│ │
│ │  • RC 보: 단부( i ) 정밀 횡단면 (외곽선 해칭, 피복선, 135° 후크 스터럽, 다단배근 점, 치수선)│ │
│ │  • RC 기둥: 횡단면 배근도 (둘레배근, Cross-tie 보조 띠철근, 주철근 번호)                     │ │
│ │  • Steel: KS H형강 단면 치수(B, H, tf, tw, r) 및 조밀/비조밀 색상 코딩                        │ │
│ └──────────────────────────────────────────────────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────────────────────────────────────────────────┐ │
│ │ [2단 뷰포트: 원본앱 참조 모듈 정의 타이틀 (예: 중앙( m ) 단면 / P-M 상관곡선)] [Fit] [Zoom]  │ │
│ │  • RC 보: 중앙( m ) 정밀 횡단면 (인장 주철근 다단배근, 압축철근, 스터럽 간격 상세)           │ │
│ │  • RC 기둥: 축력-휨 P-M 상관도 곡선 (Pn-Mn 및 φPn-φMn 곡선, 계수하중 Pu-Mu 타점, 3D 뷰)     │ │
│ │  • Steel: 횡비틀림좌굴(LTB) 모멘트 구배 다이어그램 (Cb) 및 비지지길이(Lb) 구간도             │ │
│ └──────────────────────────────────────────────────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────────────────────────────────────────────────┐ │
│ │ [3단 뷰포트: 원본앱 참조 모듈 정의 타이틀 (예: 단부( j ) 단면)]        [Fit] [Zoom] [Dim 켜기]│ │
│ │  • RC 보: 단부( j ) 정밀 횡단면 (외곽선 해칭, 피복선, 135° 후크 스터럽, 다단배근 점, 치수선)│ │
│ │  • 기타 1~2단 부재는 3단 카드를 비활성화하고 1~2단 전면 확장 지원                           │ │
│ └──────────────────────────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────────────────────────┘
```

* **전 부재군 그래픽 특성화 매트릭스**:
  1. **RC 보 (`rc_beam`)**:
     - 2단 세로 적층 뷰포트: 상단 횡단면 배근도(End-I, Mid-M, End-J 정밀 단면 및 치수선) + 하단 부재력도/길이방향 배근 포락도(BMD/SFD/Envelope). *(세부 렌더링 사양은 [`docs/15`](15_rc_beam_common_requirements_report.md) 참조)*
  2. **RC 기둥 (`rc_column`)**:
     - 1단: 횡단면 배근도 (둘레배근, Cross-tie 보조 띠철근, 주철근 번호).
     - 2단: 축력-휨 P-M 상관곡선 ($P_n-M_n$ 및 $\phi P_n-\phi M_n$, 계수하중 $P_u-M_u$ 타점 마커, 3D 토글).
  3. **철골 보 / 기둥 (`steel_beam`, `steel_column`)**:
     - 1단: KS H형강/각형강관 단면 형상 치수선, 판폭두께비 조밀/비조밀/세장 하이라이트.
     - 2단: 횡비틀림좌굴(LTB) 모멘트 구배 다이어그램 ($C_b$) 및 비지지길이($L_b$) 구간도.
  4. **철골 접합부 / 베이스플레이트 (`steel_connection`, `steel_baseplate`)**:
     - 1단: 2D CAD 평면 볼트 배열도 (게이지, 피치, 연단거리 치수선, 플레이트 외곽선).
     - 2단: 정면/측면 입면도, 콘크리트 지압선, 앵커볼트 인장 콘(Cone) 및 전단 파열면.
  5. **RC 기초 / 슬래브 (`rc_footing`, `rc_slab`)**:
     - 1단: 기초판 평면 배근망 및 말뚝(Pile) 배치 그리드.
     - 2단: 편심 하중에 따른 지반 접지압 분포도 ($q_{min} \sim q_{max}$), 위험단면 적색 점선.
  6. **PC / 보수보강 (`pc`, `rfm`)**:
     - 1단: 프리캐스트 단면(더블티, 할로우코어) 및 긴장재(Strand) 중심선 배치도.
     - 2단: CFRP 탄소섬유판/강판 접착 위치 및 보강 전/후 내력 증분 비교 차트.
* **대화형 인터랙션 & 테마 무결성**:
  - **철근/볼트 호버 툴팁**: 철근, 볼트, 텐던 요소 호버 시 직경, 배근 간격, 단면적 툴팁 표출.
  - **캔버스 테마 팔레트 (Theme-aware Palette)**: 다크/라이트 테마 전환 시 캔버스 배경(다크 `#0f172a`, 라이트 `#ffffff`), 콘크리트 외곽선, 치수선, 인출선 색상이 테마에 맞춰 즉시 동기화.
  - **DCR 버튼 툴바 간소화**: 뷰포트 내 DCR 게이지 버튼은 툴바 복잡도를 줄이기 위해 전면 배제하며, Pane 1 부재 목록 그리드 및 Pane 4 KDS 계산서의 종합 판정표에서 DCR을 명확히 제공.

### 4.4. Pane 4: KDS 표준 구조계산서 렌더러 (Right: `#pane-right-report`)
* **컴포넌트**: `report_engine.js`, `zoom_controller.js`, `report.css`, `print.css`
* **아키텍처 구조**:
  - 원본앱 출력모듈(`IDD_DGN_CHECK_MODE_DLG`, `DgnReportBase.ini`, `GENDgnReportKR.ini`)의 기능을 1:1 완벽하게 재현.
  - **레거시 계산서 모듈(`report_renderer.js`)을 완전 제거하고 `report_engine.js`로 단일화**.
  - **사용자 입력부의 [⚡ 검토] 및 [✨ 자동설계] 버튼 클릭 시 정식 원본앱 KaTeX 계산서가 즉시 출력**.

```
┌──────────────────────────────────────────────────────────────────────────────────────────────────┐
│ 계산서 상단 헤더 & 제어 툴바 (Report Control Toolbar)                                            │
├──────────────────────────────────────────────────────────────────────────────────────────────────┤
│ • 상단 헤더 액션: [−] [100%] [+] [1:1] [⛶ 폭맞춤] 계산서 확대/축소 (Ctrl+마우스 휠 지원)        │
│ • 보고서 유형   : (•) 상세 보고서 (Detail)   ( ) 요약 보고서 (Summary)                           │
│ • 출력 옵션     : [☑ 사용자 입력 데이터 상세 포함]   [☑ 단면도/P-M 그래픽 임베딩]                │
│ • 커스텀 설정   : [머릿말/회사명 설정 ⚙️]                                                        │
│ • 출력 액션     : [🖨️ 인쇄 (Print)]  [📊 Excel 내보내기 (추후 고도화 예정)]                       │
└──────────────────────────────────────────────────────────────────────────────────────────────────┘
```

* **상시 순백색(`#ffffff`) A4 고정 용지 원칙**:
  - 메인 인터페이스가 다크 테마이더라도, 계산서 용지 내부(`.a4-sheet-container`)는 실제 관공서 인쇄물과의 1:1 완벽 호환을 위해 **언제나 순백색(`#ffffff`) 배경과 고대비 텍스트(`#111827`, `#1f2937`)를 고정 유지**.
  - 패널 너비 변경 시에도 테이블과 수식이 왜곡되지 않도록 고정폭(794px / 210mm) 유지, 표준 여백 20mm, CSS Paged Media `@media print` 페이지 분할.
* **계산서 화면 확대/축소 제어기 (`zoom_controller.js`)**:
  - 상단 헤더에 `[−]`, `[100%]`, `[+]`, `[1:1 기본]`, `[⛶ 폭맞춤]` 버튼 및 확대율 레이블 제공.
  - 50% ~ 200% 스케일 범위 지원 및 `Ctrl + 마우스 휠` 줌 인/아웃 완벽 지원 (`transform: scale(...)` 기반).
* **2대 보고서 모드 분기 (라디오 버튼)**:
  1. **[요약 보고서 (Summary Report)]**:
     - 실무 퀵 검토 및 배근 현장용 1~2페이지 A4 압축 레이아웃.
     - 핵심 설계조건, 재료 제원, 2D 단면 배근도, 지배 LCB 및 종합 안전성(DCR) 검토표.
  2. **[상세 보고서 (Detail Report)] - (초정밀 공학 계산서)**:
     - **인허가 관공서 및 구조심의 제출용 정밀 공학 계산서 (최우선 중요도)**.
     - KDS 14 20 00 / 14 31 00 표준 조항 번호 1:1 명기.
     - **KaTeX LaTeX 기반 8단계 수식 유도식**:
       * 공식 기호 $\rightarrow$ 수치 대입 $\rightarrow$ 중간 계산값($a, c, \epsilon_t, \phi, V_c, V_s, \delta_{ns}$ 등) $\rightarrow$ 최종 강도 및 DCR 도출.
* **사용자 입력 정보 포함/불포함 옵션 (`IDC_DGN_REPORT_CHECK_INP`)**:
  - **`[☑ 체크 ON]` (기본값)**: 계산서 2장에 **[사용자 입력 데이터 상세]** 섹션을 자동으로 포함하여 설계자가 입력한 재료 물성치($f_{ck}, f_y$), 부재 경간, 단면 치수, 배근 상세, 계수 하중 리스트를 완전 수록.
  - **`[☐ 체크 OFF]`**: 사용자 입력 데이터 섹션을 생략하고 곧바로 [단면 특성치] 및 [KDS 상세 수식 계산]으로 진입하여 출력 장수 슬림화.
* **머릿말(Header) & 꼬릿말(Footer) 커스텀 설정 (`[머릿말 설정 ⚙️]`)**:
  - 모달 팝업을 통해 프로젝트명, 회사명(풀네임/약칭), 부재 태그, 검토자/승인자 서명란, 검토 일자 및 회사 로고 이미지를 실시간 주입.
* **출력 액션 (원본앱 1:1 부합)**:
  - **인쇄 (`[🖨️ 인쇄 (Print)]`)**: 브라우저 표준 A4 인쇄 파이프라인 (`window.print()`).
  - **엑셀 내보내기 (`[📊 Excel 내보내기]`)**: 현재 기본 계산서 내보내기 기능이 연동되어 있으며, 향후 셀 병합, KDS 표준 스타일 서식, 고해상도 수식 이미지 탑재 등 관공서 제출용 정밀 계산서 양식으로 **추후 고도화 예정**.
  - *(원본앱에 존재하지 않는 불필요한 PDF 버튼은 전면 제거)*.
* **특화 부가기능 로드맵 (CAD 도면, 물량산출, Gen 가져오기, 3D MGT)**:
  - 54종 단위부재 설계 엔진 및 KDS 계산 알고리즘의 완전한 정밀도 검증을 최우선으로 달성하기 위해, 상단 툴바의 `CAD 도면`, `물량 산출`, `Gen 가져오기` 및 사이드바의 `3D MGT` 기능은 **현재 비활성화(disabled) 처리하고 추후 고도화 예정**으로 분리 관리.
* **KDS 표준 7대 장구분 체계 확립 및 판정 표기 (요구사항 22-5-3)**:
  - 기존 구 기준 5대 장구분을 현행 KDS 기준 역학 체계에 맞추어 **7대 장구분으로 전면 개편**:
    * **제 1장. 설계 기본 정보 및 단면 제원**: 적용 설계기준(KDS 14 20), 단위계, 단면 치수($b, h$), 재료 강도($f_{ck}, f_y, f_{yt}$), 및 **2D 횡단면 배근 상세도 SVG 그래픽 임베딩**.
    * **제 2장. 설계 부재력 및 하중조합**: 계수 하중조합표, 지배 LCB 및 3-Station(단부-I, 중앙부-M, 단부-J) 설계 부재력 총괄표.
    * **제 3장. 단면 연성 및 최소철근량 검토 [신설]**: 현행 KDS 14 20 20: 2022 신기준에 따른 휨 최소철근량($\phi M_n \ge 1.2 M_{cr}$) 및 최외단 순인장변형률($\epsilon_t \ge 0.004$) 연성 한계상태 전개 (3-Station 개별 수식 전개).
    * **제 4장. 휨모멘트 강도 검토**: 3-Station 정/부모멘트 작용 시 단근/복근 설계휨강도($\phi M_n$) 8단계 수식 유도 및 DCR 판정.
    * **제 5장. 전단 및 비틀림 강도 검토**: 5.1 총괄표, 5.2 콘크리트 및 철근 전단강도($V_c, V_s, \phi V_n$), 5.3 최소 전단철근량 및 최대 배근간격, 5.4 비틀림 강도($T_{cr}, T_n$).
    * **제 6장. 사용성 한계상태 검토**: Branson 식 유효단면2차모멘트($I_e$) 3-Station 가중평균 장기처짐 및 KDS 14 20 30 균열방지 철근 간격 제한($s \le s_{\max}$).
    * **제 7장. 종합 안전성 판정 (Summary & Verdict)**: 전 항목 DCR 총괄 요약표 및 최종 엔지니어링 판정 표기:
      - `  →  O.K` 🟢 (DCR $\le 1.0$)
      - `  →  N.G` 🔴 (DCR $> 1.0$)
* **KaTeX `aligned` 환경 적용으로 A4 우측 여백 오버플로우 0건 방지 표준 (요구사항 22-5-3)**:
  - 기호식, 수치 대입, 계산 결과가 한 줄로 길어져 A4 인쇄 가로 폭(약 680px)을 벗어나는 문제를 원천 차단하기 위해, 등호(`=`) 기준으로 `\begin{aligned} ... \end{aligned}` 환경을 강제 적용하여 수직 정렬 줄바꿈 표준화.
  - 설명용 한글 괄호 태그를 제거하고 원본 고유의 간결한 `  →  O.K / N.G` 화살표 판정으로 일원화.
* **0 하중 입력 시 동적 분기 및 최소배근 상시 필수 출력 원칙 (요구사항 22-7-2)**:
  - **휨 $M_u \le 0$ 시**: 제 3장의 $\phi M_n \ge 1.2 M_{cr}$ 최소철근량 및 $\epsilon_t \ge 0.004$ 연성 검토는 **외력이 0이어도 절대로 생략하지 않고 상시 100% 정상 출력**.
  - **전단 $V_u \le 0$ 시**: 5.2절 전단강도 상세식은 1줄 요약 박스(*"작용 계수전단력 없음 ($V_u = 0.0\text{ kN}$) — 전단강도 상세 검토 생략"*)로 동적 생략하되, **5.3절 최소 전단철근량($A_{v,prov} \ge A_{v,\min}$) 및 최대 배근간격($s \le s_{\max}$) 검토는 상시 필수 출력**.
  - **비틀림 $T_u \le 0$ 시**: 상세 비틀림 계산은 간소화하되, 최소 횡방향 폐쇄스터럽($(A_v + 2A_t)_{\min}$) 및 최소 종방향 철근량($A_{l,\min}$) 검토는 유지.
* **확대율 컨트롤러 & 마우스 휠 줌 (`zoom_controller.js`)**:
  - `[-]`, `[100%]`, `[+]`, `[Fit]` 버튼 및 줌 슬라이더(50% ~ 200%) 제공 (`Ctrl + 마우스 휠` 줌 인/아웃 지원).

---

## 5. 4대 독립 리사이저 엔진 (`layout_resizer.js`)

각 분할 패널은 다른 패널에 왜곡을 주지 않고 독립적으로 크기를 조절할 수 있습니다:

| 리사이저 ID | 조절 대상 | 동작 방식 및 바운딩 제약 |
|---|---|---|
| `resizer-sidebar-h` | 좌측 사이드바 ↔ 메인 워크스페이스 | 좌우 드래그 (너비: 200px ~ 480px) |
| `resizer-left-h` | 좌측 영역(부재목록+입력폼) ↔ 중앙 그래픽 | 좌우 드래그 (좌우 분할 25% ~ 75%) |
| `resizer-left-v` | 부재 목록(Pane 1) ↔ 파라메트릭 폼(Pane 2) | 상하 드래그 (부재목록 높이: 80px ~ 400px) |
| `resizer-main-h` | 중앙 그래픽(Pane 3) ↔ 우측 계산서(Pane 4) | 좌우 드래그 (그래픽과 계산서 분할 20% ~ 80%) |

---

## 6. 디자인 시스템 토큰 (Design Tokens)

### 6.1. 테마 변수 (Color Palette)

| 토큰명 | 다크 테마 (Dark Mode) | 라이트 테마 (Light Mode) | A4 계산서 (Fixed White) |
| :--- | :--- | :--- | :--- |
| `--bg-primary` | `#0b0e14` (심해 네이비) | `#f8fafc` (소프트 화이트) | `#ffffff` (순백색 고정) |
| `--bg-secondary` | `#121722` (다크 카드) | `#ffffff` (순백색) | `#ffffff` |
| `--bg-surface` | `#20293a` (엘리베이션) | `#e2e8f0` (연회색) | `#f8fafc` (수식 블록) |
| `--border` | `#2b364c` | `#cbd5e1` | `#e5e7eb` (테이블 선) |
| `--text-main` | `#f1f5f9` (고대비 백색) | `#0f172a` (진한 네이비) | `#111827` (선명한 블랙) |
| `--text-muted` | `#94a3b8` | `#64748b` | `#4b5563` (보조 수식) |
| `--accent` | `#3b82f6` (블루) | `#2563eb` (로열 블루) | `#1d4ed8` (강조 링크) |
| `--success` | `#10b981` (안전/합격) | `#059669` | `#047857` (DCR $\le 1.0$) |
| `--danger` | `#ef4444` (초과/NG) | `#dc2626` | `#b91c1c` (DCR $> 1.0$) |

### 6.2. 타이포그래피 (Typography)
* **기본 본문 폰트**: `'Inter', -apple-system, BlinkMacSystemFont, sans-serif`
* **헤딩 및 타이틀**: `'Outfit', sans-serif`
* **구조 수식 및 코드**: `'Fira Code', 'Cascadia Code', monospace`

---

## 7. 키보드 단축키 매핑 (Global Shortcuts)

* `Ctrl + K`: 상단 빠른 모듈 검색창 포커스
* `Ctrl + B`: 좌측 사이드바 접기 / 펼치기 토글
* `Ctrl + S`: 프로젝트 전체 데이터 JSON 내보내기 (Export)
* `Ctrl + Wheel` (계산서 영역): A4 계산서 실시간 확대/축소 (50% ~ 200%)
* `Esc`: 모듈 검색창 닫기 및 팝업 닫기

---

## 8. 프론트엔드 모듈 및 다형적 모듈팩 아키텍처 (Polymorphic Architecture)

부재별 상이한 입력 파라미터, 2D 그래픽 뷰, KDS 계산 수식의 이질성을 완벽히 수용하기 위해, `src/web/static/js/`는 코어 프레임워크와 부재별 독립 모듈팩(`modules/`)으로 분리 패키징됩니다:

```
src/web/static/js/
├── core/
│   ├── dispatcher.js              # 4-Pane 모듈 스위칭 라이프사이클 관리 (ModuleDispatcher)
│   ├── event_bus.js               # 100ms 실시간 데이터 동기화 버스 (EventBus)
│   ├── modal_manager.js           # 원본 서브 대화창('...') 공통 팝업 매니저
│   └── report_engine.js           # KaTeX 수식, 머릿말 주입, A4 인쇄/PDF 제어기
├── components/
│   ├── layout_resizer.js          # 4대 독립 리사이저 엔진 (resizer-sidebar/left/main)
│   ├── member_manager.js          # Pane 1 다중 부재 CRUD 및 실시간 요약 그리드
│   ├── form_generator.js          # Pane 2 파라메트릭 인풋 폼 동적 빌더
│   └── form_combobox.js           # KS 철근/강재/콘크리트 표준 콤보 위젯
├── store/
│   ├── project_store.js           # 전역 상태 SSOT & LocalStorage 영속화
│   └── unit_manager.js            # SI / MKS / US 4대 단위계 자동 환산
└── modules/                       # [부재별 전용 모듈 팩 번들]
    ├── rc_beam/                   # RC 보 모듈 (form_rc_beam.js, canvas_rc_beam.js, redcr_rc_beam.js)
    ├── rc_column/                 # RC 기둥 모듈 (form_rc_column.js, canvas_rc_column.js, redcr_rc_column.js)
    ├── rc_footing/                # RC 기초 모듈 (form_rc_footing.js, canvas_rc_footing.js, redcr_rc_footing.js)
    ├── steel_beam/                # 철골 보 모듈 (form_steel_beam.js, canvas_steel_beam.js, redcr_steel_beam.js)
    ├── steel_baseplate/           # 베이스플레이트 모듈 (form_baseplate.js, canvas_baseplate.js, redcr_baseplate.js)
    └── ...                        # (기타 단위부재 순차 확장)
```

### 8.1. 주요 컴포넌트 매핑 및 역할 정의

| 기능 레이어 | 핵심 파일 경로 | 주요 역할 및 담당 인터페이스 |
|---|---|---|
| **메인 템플릿** | `src/web/templates/index.html` | 4-Pane 워크스페이스, 상단 마스터 툴바, 스마트 사이드바 마크업 |
| **CSS 스타일** | `src/web/static/css/*.css` | `theme.css`, `layout.css`, `components.css`, `canvas.css`, `report.css`, `print.css` |
| **코어 디스패처** | `src/web/static/js/core/dispatcher.js` | 부재 전환 시 50ms 이내 4-Pane(폼/캔버스/계산서) 동시 스위칭 |
| **실시간 이벤트 버스**| `src/web/static/js/core/event_bus.js` | 폼 입력값 변경 시 **100ms 이내 3-View(Canvas/Report/List) 동시 동기화** |
| **서브모달 매니저** | `src/web/static/js/core/modal_manager.js` | 원본 1:1 서브 대화창(`...`) 팝업 모달 생명주기 관리 |
| **리포트 엔진** | `src/web/static/js/core/report_engine.js` | 순백색 A4 용지 고정, 상세/요약 분기, KaTeX 8단계 수식 렌더러 |
| **4대 리사이저** | `src/web/static/js/components/layout_resizer.js` | 사이드바, 좌측상하, 좌우메인 4대 독립 리사이저 엔진 |
| **부재 관리자** | `src/web/static/js/components/member_manager.js` | Pane 1 다중 부재 CRUD, 인라인 이름 변경, DCR 상태 글자 그리드 |
| **파라메트릭 폼** | `src/web/static/js/components/form_generator.js` | Pane 2 4대 서브탭, KS 콤보박스, 3버튼(`적용/검토/설계`) 파이프라인 |
| **중앙 데이터스토어**| `src/web/static/js/store/project_store.js` | 전역 단일 진실 공급원(SSOT), JSON 프로젝트 I/O, `localStorage` 영속화 |
| **단위계 매니저** | `src/web/static/js/store/unit_manager.js` | SI / MKS / US 4대 단위계 0.01초 자동 환산기 |

---

# [PART 2] 원본 ↔ 모던 웹 1:1 매핑 핵심 원칙 및 브릿지

> [!NOTE]
> **🏛️ 원본 데스크톱앱 역공학 기술 분석 명세서 독립 분리 안내 (SSOT Separation)**:  
> 원본 원본앱(Design+.exe)의 MFC/BCGControlBar 윈도우 프레임워크, Menu.ini 11개 탭 리본 메뉴 구성, 4대 폼뷰(CMainFormView*), 3대 인터랙션 모드, 부재별 DLG_*.ini 다이얼로그 리소스 및 DPLUS_VDraw.dll 드로잉 엔진 분석 등 **데스크톱 역공학 기술 명세 및 백엔드 라우팅 사양은 전용 기술 문서인 [docs/13_original_app_ui_specification.md](13_original_app_ui_specification.md)에서 단독 관리**합니다.

## 9. 원본 ↔ 모던 웹 1:1 매핑 핵심 원칙

| 원본 데스크톱앱 (Design+.exe) | AltDP 모던 웹 워크스페이스 | 웹 UI/UX 구현 핵심 원칙 | 상세 기술 명세 링크 |
|---|---|---|:---:|
| **WorkTree Dock** (좌측 탐색기) | **Left Sidebar** (#sidebar-nav) | 8대 카테고리 Pills 탭, ⭐ 즐겨찾기 고정, 3단계 계층 WorkTree, OK/NG 글자 표기 | [상세보기](13_original_app_ui_specification.md#1-원본앱-윈도우-프레임워크-구조) |
| **CMainFormViewList** (부재 목록) | **Pane 1: 부재 매니저** (#pane-member-list) | 층별/부재별 CRUD, 인라인 이름 변경, 단면/배근/DCR 요약 그리드 | [상세보기](13_original_app_ui_specification.md#3-원본-4대-메인-폼뷰-cmainformview-구조) |
| **IDD_*_PMODE_DLG** (다이얼로그) | **Pane 2: 파라메트릭 폼** (#pane-input-form) | 모듈 주도형 4대 서브탭, 32px 컴팩트 Sticky 플로팅바, 배근유형 라디오, 주근 분리입력, 12포인트 순간격 검토 | [상세보기](13_original_app_ui_specification.md#5-부재별-원본-다이얼로그-폼-구조-dlg_ini-분석) |
| **DPLUS_VDraw.dll** (2D/3D 드로잉) | **Pane 3: 그래픽 정보부** (#pane-graphic-view)| 1~3단 동적 세로 적층 뷰포트 (상단 횡단면 배근도 + 하단 길이방향 역학도), 카드별 독립 줌/팬/Fit | [상세보기](13_original_app_ui_specification.md#6-원본-2d3d-드로잉-엔진-분석-dplus_vdrawdll) |
| **DgnReportBase.ini** (계산서) | **Pane 4: KDS 구조계산서** (#pane-right-report) | 상시 순백색(#ffffff) A4 용지, KDS 7대 장구분, KaTeX aligned 줄바꿈, 0하중 방어로직 | [상세보기](13_original_app_ui_specification.md#7-원본-계산서-생성-체계-dgnreportbaseini-gendgnreportkrini) |

---

# [PART 3] 부재별 수직 관통(Vertical Slice) 5대 공정과 UI/UX 연동 표준 (요구사항 21, 22 기반)

## 10. 2단계 5정밀 마이크로 공정(Step 1~5)과 UI/UX 구성요소 1:1 결합 사양

`docs/16`(Goal 마이크로 공정 표준 실행 지침) 및 `요구사항 22`(RC 보 수직 관통 플래그십)에서 정립된 **2단계 5정밀 마이크로 공정**은 부재 설계 로직뿐만 아니라 모던 웹 UI/UX 레이어와 1:1로 긴밀하게 결합되어 있습니다. 향후 61종 전 단위부재의 수직 구현 시 아래 매핑 표준을 반드시 준수해야 합니다.

### 10.1. Step별 UI/UX 컴포넌트 1:1 결합 매트릭스

| 마이크로 공정 | 권장 모델 | UI/UX 워크스페이스 타겟 | 핵심 프론트엔드 연동 산출물 | 상호 인터랙션 & 데이터 흐름 |
|:---:|:---:|---|---|---|
| **Step 1**<br>KDS 계산 엔진 & Pydantic 스키마 | 🧠 **High** | `src/engine/`<br>`src/api/` | • 부재 입출력 Pydantic 스키마 정의<br>• DCR 산출 및 한계상태 검토 API (`POST /api/design/...`) | UI/UX와 백엔드 간의 **데이터 계약(Data Contract)** 확립. 단위계 자동 환산 및 유효성 검증 베이스라인 제공 |
| **Step 2**<br>원본앱 1:1 서브탭 입력폼 & 모달 | ⚙️ **Medium** | **Pane 2** (Left-Sub 하단)<br>`#pane-input-form` | • 원본앱 `DLG_*.ini` 기반 모듈 정의 서브탭 (1~5개 가변 탭)<br>• **배근 유형(Arrange Type) 3대 라디오 옵션** (전단면 / 양단부대칭 / 3단면 독립)<br>• **주철근 복합 분리입력 컨트롤** (`[개수] + [-] + [호칭경 콤보]`, 1단 $\ge 2$, 2단 $\ge 0$)<br>• **배근 유형 연동 부재력 테이블 동적 `disabled` 및 대칭 동기화**<br>• **12포인트 철근 순간격($s_{clear}$) 실시간 전수 검토 뱃지 & 팝오버**<br>• **전 모듈 상단 Sticky 플로팅 바 & 32px 컴팩트 액션 버튼 (`적용/검토/설계`) 표준 적용**<br>• KS 표준 콤보박스 (`form_combobox.js`)<br>• 리사이즈 반응형 레이아웃 방어 (`min-width: 320px`, 컨트롤 `min-width: 110px`)<br>• 원본 1:1 서브 대화창(`...`) 모달 연동 | 사용자의 파라메트릭 제원/재료/배근 입력. 3버튼(`적용/검토/설계`)을 통한 `ProjectStore` 메모리 즉각 바인딩 |
| **Step 3**<br>2D VDraw 캔버스 배근도 & 그래픽스 | ⚙️ **Medium** | **Pane 3** (Center)<br>`#pane-graphic-view` | • **동적 가변 세로 적층형 다중 뷰포트** (`canvas_[module].js`)<br>  - 부재 모듈별 원본 명칭 정의 (예: 상단 횡단면 배근도 + 하단 길이방향 모멘트/전단력도/배근도)<br>  - **표피/비틀림 철근 널 병합(`?? 0`) 패치**: 0개 입력 시 비틀림 철근 렌더링 스킵<br>  - 세로 분할 카드별 독립 줌/팬/Fit 인터랙션 및 잔존 모듈 배제 | `ProjectStore` 입력값 변경 시 캔버스 실시간 재렌더링. 휠 줌/팬, 요소 호버 툴팁 인터랙션 제공 |
| **Step 4**<br>A4 7대장구분 8단계 KaTeX 계산서 | 🧠 **High** | **Pane 4** (Right Dock)<br>`#pane-right-report` | • **상시 순백색(`#ffffff`) A4 고정 용지** (`report_engine.js` 단일화)<br>• 상세/요약 라디오 분기 및 입력데이터 포함 토글<br>• **KDS 7대 장구분 체계** (제 3장 연성검토 신설) 및 KaTeX LaTeX 8단계 수식 전개식<br>• **KaTeX `aligned` 줄바꿈 적용으로 A4 우측 오버플로우 0건 방지**<br>• **0 하중 동적 분기 및 최소배근($\phi M_n \ge 1.2 M_{cr}$, $A_{v,\min}$, $s_{\max}$) 상시 필수 출력**<br>• 원본 고유 `  →  O.K / N.G` 판정 화살표<br>• **[🖨️ 인쇄] & [📊 Excel 내보내기] 연동 (PDF 버튼 제거)** | [⚡ 검토] / [✨ 설계] 클릭 시 백엔드 검토 결과와 수식을 조립하여 관공서 심의 수준의 정밀 계산서 실시간 렌더링 |
| **Step 5**<br>4열 통합 E2E 검증 & 온라인 오픈 | ⚙️ **Medium** | **전체 4-Pane 워크스페이스**<br>`core/dispatcher.js` | • `ModuleDispatcher`를 통한 4-Pane 모듈 스위칭 라이프사이클<br>• `EventBus` 중심 **100ms 이내 실시간 3-View 동시 동기화**<br>• `catalog.js`에서 해당 부재 `is_wip: false` 정식 온라인 전환 | 탐색기 트리, 부재 매니저, 입력폼, 캔버스, 계산서의 전수 E2E 검증 및 콘솔 에러 0건 확인 후 실사용 배포 |

---

## 11. 부재별 수직 구현계획(요구사항 문서) 수립 시 UI/UX 필수 명세 기준 (Checklist)

구조 엔지니어링 에이전트가 새로운 부재의 수직 관통 구현계획(예: `요구사항 23 RC 기둥`, `요구사항 24 RC 전단벽`, `요구사항 25 철골 보` 등)을 수립하거나 요구사항 문서를 작성할 때, **백엔드 수식뿐만 아니라 프론트엔드 UI/UX 사양을 반드시 선행 검토하고 명세서에 포함**해야 합니다:

### 🌟 요구사항 문서 작성 시 필수 UI/UX 검토 항목 (Mandatory UI/UX Checklist)
1. **[사이드바 & 카테고리 정합성]**:
   - 해당 부재가 8대 카테고리 Pills 탭 중 어느 그룹에 속하며, 즐겨찾기(⭐) 및 3단계 WorkTree(`Level 1~3`)에서 어떻게 식별되는가?
2. **[Pane 1 부재 매니저 요약 테이블]**:
   - 부재 목록 테이블에 표시될 핵심 요약 컬럼(단면 치수, 주요 배근/규격, 지배 계수 부재력, DCR 글자 표기)이 명확히 정의되었는가?
3. **[Pane 2 모듈 주도형 서브탭 & 배근 컨트롤 & 반응형 방어 & 플로팅 바]**:
   - 원본앱 `DLG_*.ini`의 입력 컨트롤들을 바탕으로 서브탭 개수와 명칭을 어떻게 구성할 것인가?
   - **배근 유형(Arrange Type) 라디오 옵션 및 주근 복합 분리입력(`개수-호칭경`) 컨트롤이 설계되었는가?**
   - **부재력 테이블의 행/셀 동적 `disabled` 및 대칭 동기화가 연동되었는가?**
   - **철근 순간격(Clear Spacing) 실시간 검토 뱃지와 상세 팝오버가 지원되는가?**
   - 유효폭($b_{eff}$), 장주 계수($\delta_{ns}$), 단면 DB 브라우저 등 부재 고유의 서브 대화창(`...`) 모달이 식별되었는가?
   - 좌우 패널 리사이즈 시 인풋 박스가 찌그러지지 않도록 반응형 래핑과 컨트롤 최소 폭(`min-width: 110px`)이 설계되었는가?
   - **전 모듈 공통 상단 Sticky 플로팅 바(`position: sticky; top: 0; z-index: 25;`) 및 32px 슬림 액션 버튼이 적용되었는가?**
4. **[Pane 3 동적 가변 1~3단 그래픽 뷰포트]**:
   - 해당 부재의 뷰포트 단수(1단, 2단, 3단)와 각 단의 명칭(원본앱 기준 용어: 예: `단면 배근도`, `부재력도/배근도` 등)이 무엇인가?
   - 표피/비틀림 철근 등 선택적 요소의 널 병합(`?? 0`) 및 0개 시 렌더링 스킵이 처리되었는가?
   - 세로 분할 캔버스에 최적화하여 횡단면, P-M곡선, 상세도가 큼직하고 시원하게 렌더링되도록 설계되었는가? (잔존 임시 그래픽 배제)
5. **[Pane 4 순백색 A4 KDS 7대장구분 8단계 KaTeX 구조계산서 & Excel 연동]**:
   - 상세 보고서(Detail)와 요약 보고서(Summary)의 구성 차이가 정의되었는가?
   - `[☑ 사용자 입력 데이터 상세 포함]` 토글 시 2장에 어떤 입력 파라미터가 표출되는가?
   - **KDS 7대 장구분(제 3장 연성검토 신설) 및 등호(=) 기준 `aligned` 줄바꿈(오버플로우 0건)이 적용되었는가?**
   - **0 하중 입력 시 동적 분기(전단 상세 생략) 및 최소배근($\phi M_n \ge 1.2 M_{cr}, A_{v,\min}, s_{\max}$) 상시 필수 출력이 설계되었는가?**
   - [⚡ 검토] / [✨ 자동설계] 클릭 시 정식 계산서가 즉시 출력되며, [📊 Excel 내보내기]가 정상 지원되는가?
6. **[실시간 반응성 및 성능]**:
   - 파라메터 수정 시 100ms 이내에 캔버스와 계산서가 렉(Lag) 없이 비동기 갱신되는가?

